/*

Desenvolver uma função que imprime todos os elementos
de um vetor de tamanho n passado como parâmetro.

Elementos repetidos não devem ser impressos. Para cada
elemento vet[i], verifique se os elementos anteriores a
ele (ou seja, nos  ́ındices 0 ≤ j ≤ i − 1) já foram impressos.

Determine a complexidade do algoritmo.

void imprimeSemRepeticao(int vet[], int n);

*/