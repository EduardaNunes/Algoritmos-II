/*

Altere o Selection sort da seguinte forma:

▶ Crie uma função ̃int menor(int vet[], int n);
para encontrar e retornar o  ́ındice do menor elemento de
um vetor vet de tamanho n passados como parâmetros.

▶ Implemente uma versão recursiva do Selection sort que, a
cada chamada recursiva, usa a função ̃menor para
determinar o menor  ́ındice e fazer a troca.

*/