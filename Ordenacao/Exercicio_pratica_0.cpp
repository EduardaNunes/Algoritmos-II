/* 

Desenvolver duas funções para alocar e retornar um vetor
de n elementos, sendo n passado como parâmetro.

a.  int* vetorAleatorio(int n): retorna um vetor
    composto de valores inteiros aleatórios;

b.  int* vetorDecrescente(int n): retorna um vetor
    composto de valores inteiros em ordem decrescente.

Em seguida, implemente os algoritmos de ordenação
Bubble sort, Selection sort e Insertion sort, conforme vistos
em aula, e teste-os com os vetores gerados pelas funções acima.

Obs: para obter um valor aleatório, use o código abaixo:

#include <cstdlib>
int numeroAleatorio(int m)
{
    // retorna um valor no intervalo [0, m-1]
    return rand() % m;
}

*/